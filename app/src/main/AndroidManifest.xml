<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.play.eg.navigationcomponent">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NavigationComponent">
        <activity
            android:name=".NavigationDrawer.MainActivity"
            android:theme="@style/Theme.NavigationComponent.NoActionBar"
            android:exported="false" />
        <activity
            android:name=".bottomnavigationview.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ViewPager2.MainActivity"
            android:exported="false" />
        <activity
            android:name=".BottomSheet.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>